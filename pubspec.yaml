name: pinkrain
description: "Health Tracking App"
publish_to: 'none'

version: 2.3.4+1

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_svg: ^2.2.3
  go_router: ^16.3.0
  flutter_riverpod: ^2.6.1
  intl: ^0.20.2
  percent_indicator: ^4.2.4
  pretty_animated_text: ^2.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.5
  cristalyse: ^1.17.3
  tflite_flutter: ^0.12.1
  pdf: ^3.10.7
  share_plus: ^11.0.0
  flutter_local_notifications: ^19.5.0
  permission_handler: ^12.0.0+1
  just_audio: ^0.10.5
  url_launcher: ^6.2.5
  http: ^1.6.0
  html: ^0.15.4
  timezone: ^0.10.1
  hugeicons: ^1.1.5
  hux: ^0.24.2
  package_info_plus: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3
  flutter_lints: ^6.0.0
  mockito: ^5.6.3
  build_runner: ^2.11.1

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  remove_alpha_ios: true
  image_path: "assets/fonts/logo/logo.png"

flutter:

  uses-material-design: true

  assets:
    - assets/icons/
    - assets/audio-tracks/
    # - assets/models/
    # - assets/models/symptom_prediction/model.tflite 
    # - assets/models/symptom_prediction/tokenizer.json
    # - assets/models/symptom_prediction/mlb.json

  fonts:
    - family: Outfit
      fonts:
        - asset: assets/fonts/Outfit.ttf